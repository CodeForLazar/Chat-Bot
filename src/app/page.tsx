import Image from 'next/image';

export default function Home() {
   return (
      <main className='absolute inset-0 flex h-screen w-screen items-center justify-center  bg-slate-900'></main>
   );
}
